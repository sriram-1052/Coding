#include<stdio.h>
#include <stdlib.h>
#include<string.h>
int main()
{
char a[100],b[100],i,j,n1,n2,flag=1;
scanf("%s\n%s",&a,&b);
n1=strlen(a);
n2=strlen(b);
for(i=0;i<n1;i++)
{
    if((a[i]>=65)&&(a[i]<=90))
    {
        continue;
    }
    else
    {
        flag=0;
        break;
    }
}

return 0;
}


printf("%s",&a[1]) is used to print the string from 1st position till end
%% is used to print % in output
scanf("%[^\n]"); will get string till new line
