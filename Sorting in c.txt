
/*
#include <stdio.h>
#include <conio.h>
void main()
{
    int n,a[200],i,j,temp=0,x,z;
    printf("Enter the total no.of elements:\n");
    scanf("%d",&n);
    printf("Enter the elements:\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        for(j=0;j<n;j++)
        {
            if(a[i]>a[j])
            {
                temp=a[i];
                a[i]=a[j];
                a[j]=temp;
            }
        }
    }
    printf("The sorted order is:");
    for(i=0;i<n;i++)
    {
        printf("%d ",a[i]);
    }
    printf("\n");
    printf("Enter the position:\n");
    scanf("%d",&x);
    z=a[0];
    for(i=0;i<(x-1);i++)
    {
        a[i]=a[i+1];
    }
    a[x-1]=z;
    printf("The required answer is:\n");
    for(i=0;i<n;i++)
    {
        printf("%d ",a[i]);
    }
    getch();
}
*/



#include <stdio.h>
#include <conio.h>
void main()
{
    int n,a[200],i,j,temp=0,x,z;
    printf("Enter the total no.of elements and the position:");
    scanf("%d %d",&n,&x);
    printf("Enter the elements:");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    if((2<=n<=1000)&&(1<=x<=n))
    {
    for(i=0;i<n;i++)
    {
        for(j=0;j<n;j++)
        {
            if(a[i]>a[j])
            {
                temp=a[i];
                a[i]=a[j];
                a[j]=temp;
            }
        }
    }
    printf("The sorted order is: ");
    for(i=0;i<n;i++)
    {
        printf("%d ",a[i]);
    }
    printf("\n");
    z=a[0];
    for(i=0;i<(x-1);i++)
    {
        a[i]=a[i+1];
    }
    a[x-1]=z;
    printf("The required answer is: ");
    for(i=0;i<n;i++)
    {
        printf("%d ",a[i]);
    }
    }
    getch();
}





Output:
Enter the total no.of elements and the position:5 3                                                                                           
Enter the elements:55 10 25 47 73                                                                                                             
The sorted order is: 73 55 47 25 10                                                                                                           
The required answer is: 55 47 73 25 10